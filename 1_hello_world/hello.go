// Tells Go Compiler to build executable instead of a shared library
package main

// Used for printing
import "fmt"

// Main function is entry point of execution
func main() {
	fmt.Println("Hello, world")
}
